# A2A Peer Coordination

1. Check who's online: `chat_peers()`
2. Send a specific message via `chat_send()` with context and expected action
3. Wait for reply (arrives as `[chat from <id>]` follow-up)
4. Act on the response

Keep messages short and specific.
